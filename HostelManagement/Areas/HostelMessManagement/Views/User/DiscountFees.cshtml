@model HostelManagement.Areas.HostelMessManagement.Models.StudentSearchViewModel
@{
    ViewBag.Title = "DiscountFees";
}

<h2>DiscountFees</h2>

@using (Ajax.BeginForm("DiscountFees", null, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "feeDiscountId", InsertionMode = InsertionMode.Replace }, new { @class = "form-horizontal" }))
{
    <div class="form-group">
        @Html.LabelFor(m => m.bid)
        @Html.TextBoxFor(m => m.bid, new { @class = "form-control" })
    </div>
    <input type="submit" class="btn btn-primary" />
}

<div id="feeDiscountId">

</div>
