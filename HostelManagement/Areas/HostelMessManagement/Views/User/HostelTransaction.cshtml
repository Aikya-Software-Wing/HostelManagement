@model HostelManagement.Areas.HostelMessManagement.Models.StudentSearchViewModel
@{
    ViewBag.Title = "HostelTransaction";
}

<h2>HostelTransaction</h2>

@using (Ajax.BeginForm("HostelTransaction", null, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "hostelTransactionId", InsertionMode = InsertionMode.Replace }, new { @class = "form-horizontal" }))
{
    <div class="form-group">
        @Html.LabelFor(m => m.bid)
        @Html.TextBoxFor(m => m.bid, new { @class = "form-control" })
    </div>
    <input type="submit" class="btn btn-primary" />
}

<div id="hostelTransactionId">

</div>
